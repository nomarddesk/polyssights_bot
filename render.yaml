# render.yaml
services:
  - type: web
    name: polyssights-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    pythonVersion: 3.11.0  # Use a stable, widely supported version
